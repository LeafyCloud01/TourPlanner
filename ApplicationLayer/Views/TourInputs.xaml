<UserControl x:Class="PresentationLayer.Views.TourInputs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationLayer.Views"
             xmlns:local1="clr-namespace:PresentationLayer.ViewModels"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local1:TourInputsViewModel/>
    </UserControl.DataContext>
    

        <ScrollViewer>
        <StackPanel>

            <Label>Tour Name</Label>
            <TextBox x:Name="TourNameInput" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Label>Description</Label>
            <TextBox x:Name="TourDescriptionInput" Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Label>From</Label>
            <TextBox x:Name="TourFromInput" Text="{Binding From, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Label>To</Label>
            <TextBox x:Name="TourToInput" Text="{Binding To, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Label>Transport Type</Label>
            <ComboBox x:Name="TourTransportTypeInput" ItemsSource="{Binding TransportTypes}" SelectedItem="{Binding Path=TransportType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></ComboBox>
            <Label>Distance</Label>
            <Label>Estimated Time</Label>
            <Label>Route Information</Label>

            <Button x:Name="SubmitTour" Command="{Binding ChangeTour}">Submit</Button>

        </StackPanel>
    </ScrollViewer>
</UserControl>
